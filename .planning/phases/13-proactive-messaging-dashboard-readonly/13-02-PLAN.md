---
phase: 13-proactive-messaging-dashboard-readonly
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/layout.tsx
autonomous: true
requirements:
  - WDSH-01

must_haves:
  truths:
    - "Hotel owner can still access the existing web dashboard with all conversation history and hotel configuration visible"
    - "Hotels that completed Telegram onboarding see an informational banner indicating Telegram is the primary interface"
    - "No dashboard write features are disabled — all existing functionality remains intact"
  artifacts:
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Telegram-first informational banner for onboarded hotels"
      contains: "owner_telegram_chat_id"
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "hotels.owner_telegram_chat_id"
      via: "conditional banner rendering based on Telegram onboarding status"
      pattern: "owner_telegram_chat_id"
---

<objective>
Add a "Telegram-first" informational banner to the dashboard for hotels that have completed Telegram onboarding, satisfying WDSH-01 (existing dashboard remains accessible as readonly optional view).

Purpose: Hotel owners who primarily use Telegram see a non-blocking banner indicating Telegram is the primary interface, while the full dashboard remains accessible and functional.

Output: Updated `layout.tsx` with conditional informational banner.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-proactive-messaging-dashboard-readonly/13-RESEARCH.md

# Dashboard layout with existing banner pattern:
@src/app/(dashboard)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Telegram-first informational banner to dashboard layout</name>
  <files>src/app/(dashboard)/layout.tsx</files>
  <action>
In `src/app/(dashboard)/layout.tsx`, add a "Telegram-first" informational banner AFTER the existing onboarding banner block (line 77-84) and BEFORE the header element.

The banner is shown ONLY when `typedHotel.owner_telegram_chat_id` is not null (hotel owner has completed Telegram onboarding via Setup Wizard).

**Banner JSX — insert between the onboarding banner closing `)}` and the `{/* Top header */}` comment:**

```tsx
{/* Telegram-first banner — shown when hotel has completed Telegram onboarding */}
{typedHotel.owner_telegram_chat_id && (
  <div className="bg-blue-50 border-b border-blue-100 px-4 py-2 text-center text-sm text-blue-700">
    Your primary interface is now Telegram. This dashboard shows your conversation
    history and hotel configuration as a readonly view.
  </div>
)}
```

**Important constraints:**
- Do NOT disable any write features (employee toggles, knowledge base CRUD, settings, etc.) — WDSH-01 says "accessible as readonly optional view", meaning the dashboard is the secondary view, not that writes must be blocked
- Do NOT modify any navigation links, routes, or form components
- Do NOT add any `disabled` props to any existing components
- The banner is purely informational — no redirect, no modal, no blocking behavior
- Use `bg-blue-50` / `text-blue-700` — blue tones for informational (not warning) messages, consistent with the onboarding banner using `bg-primary` for action-required and blue for information
- The banner renders below the onboarding banner (if both conditions are true, both banners show — but in practice a hotel with owner_telegram_chat_id has already completed onboarding, so the onboarding banner will not show)
  </action>
  <verify>
Run `npx tsc --noEmit` — layout.tsx compiles without errors. Visually inspect the file: the banner is conditionally rendered based on `owner_telegram_chat_id`, uses blue-50/blue-700 styling, and no other changes were made to the layout.
  </verify>
  <done>
Dashboard layout.tsx has a conditional "Telegram-first" informational banner shown only for hotels with owner_telegram_chat_id set. No dashboard functionality is disabled. All existing navigation, forms, and features remain intact. WDSH-01 satisfied.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes — layout.tsx compiles
2. Banner is conditional on `typedHotel.owner_telegram_chat_id` (not always shown)
3. No `disabled` props added anywhere
4. No navigation links removed or modified
5. No routes blocked or redirected
6. All existing dashboard pages remain functional
</verification>

<success_criteria>
- Telegram-onboarded hotels see a blue informational banner at the top of the dashboard
- Non-onboarded hotels see no change to the dashboard
- All dashboard write features (employee toggles, knowledge base, settings, billing) remain fully functional
- WDSH-01 requirement satisfied: dashboard accessible as readonly optional view
</success_criteria>

<output>
After completion, create `.planning/phases/13-proactive-messaging-dashboard-readonly/13-02-SUMMARY.md`
</output>
