---
phase: 12-billing-model-migration-and-trial-end-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0011_billing_v2.sql
  - src/lib/billing/plans.ts
  - src/lib/billing/enforcement.ts
  - src/types/database.ts
  - src/lib/telegram/wizard/wizardActions.ts
autonomous: true
requirements: [PRIC-01, PRIC-02]

must_haves:
  truths:
    - "Each AI employee role has its own monthly price defined in EMPLOYEE_ROLE_PRICES"
    - "Hotel owner's Telegram chat_id is persisted to the hotels table on wizard completion"
    - "Subscriptions table has trial notification tracking columns for cron idempotency"
    - "Trial remains 14 days with all employees active (seed_hotel_defaults unchanged)"
  artifacts:
    - path: "supabase/migrations/0011_billing_v2.sql"
      provides: "owner_telegram_chat_id on hotels, trial notification columns on subscriptions"
      contains: "owner_telegram_chat_id"
    - path: "src/lib/billing/plans.ts"
      provides: "Per-employee pricing constants"
      exports: ["EMPLOYEE_ROLE_PRICES", "EmployeeRoleKey"]
    - path: "src/lib/billing/enforcement.ts"
      provides: "Updated enforcement for per-employee model"
      exports: ["enforceAgentLimit"]
    - path: "src/types/database.ts"
      provides: "Updated Hotel and Subscription interfaces"
      contains: "owner_telegram_chat_id"
    - path: "src/lib/telegram/wizard/wizardActions.ts"
      provides: "chat_id persistence in completeWizard"
      contains: "owner_telegram_chat_id"
  key_links:
    - from: "src/lib/telegram/wizard/wizardActions.ts"
      to: "hotels.owner_telegram_chat_id"
      via: "supabase update in completeWizard"
      pattern: "owner_telegram_chat_id.*chatId"
    - from: "src/lib/billing/plans.ts"
      to: "src/lib/billing/enforcement.ts"
      via: "EMPLOYEE_ROLE_PRICES import"
      pattern: "EMPLOYEE_ROLE_PRICES"
---

<objective>
Create the database migration and per-employee pricing foundation for the billing model transition.

Purpose: Phase 12 replaces tier-based billing (Starter/Pro/Enterprise) with per-employee pricing. This plan lays the database and pricing infrastructure that all subsequent plans depend on: the owner's Telegram chat_id for cron notifications, trial notification tracking columns for idempotency, and per-role pricing constants.

Output: Migration 0011_billing_v2.sql, updated plans.ts with EMPLOYEE_ROLE_PRICES, updated TypeScript types, chat_id persistence in wizard completion.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-billing-model-migration-and-trial-end-flow/12-RESEARCH.md
@.planning/phases/06-billing/06-01-SUMMARY.md
@.planning/phases/11-setup-wizard-bot/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration and per-employee pricing constants</name>
  <files>
    supabase/migrations/0011_billing_v2.sql
    src/lib/billing/plans.ts
    src/lib/billing/enforcement.ts
    src/types/database.ts
  </files>
  <action>
    Create migration 0011_billing_v2.sql with two schema changes:

    1. Add owner_telegram_chat_id BIGINT column to hotels table:
       ALTER TABLE public.hotels ADD COLUMN owner_telegram_chat_id BIGINT;
       No NOT NULL constraint — column is NULL for hotels not yet onboarded via Telegram wizard.

    2. Add trial notification tracking columns to subscriptions table:
       ALTER TABLE public.subscriptions ADD COLUMN trial_notified_day_7 BOOLEAN NOT NULL DEFAULT FALSE;
       ALTER TABLE public.subscriptions ADD COLUMN trial_notified_day_12 BOOLEAN NOT NULL DEFAULT FALSE;
       ALTER TABLE public.subscriptions ADD COLUMN trial_notified_day_13 BOOLEAN NOT NULL DEFAULT FALSE;
       ALTER TABLE public.subscriptions ADD COLUMN trial_notified_day_14 BOOLEAN NOT NULL DEFAULT FALSE;
       These columns prevent duplicate cron notifications (idempotency pattern from research).

    Update src/lib/billing/plans.ts:
    - Keep existing PLAN_NAMES, PlanName, PLAN_LIMITS, PLAN_PRICES, getProviderForHotel (backward compatible)
    - Add new per-employee pricing constants:

      export type EmployeeRoleKey = 'front_desk' | 'booking_ai' | 'guest_experience' | 'housekeeping_coordinator';

      export const EMPLOYEE_ROLE_PRICES: Record<EmployeeRoleKey, { try: number; eur: number; displayName: string; shortCode: string }> = {
        front_desk:               { try: 149, eur: 15, displayName: 'Front Desk',    shortCode: 'fd' },
        booking_ai:               { try: 149, eur: 15, displayName: 'Booking AI',    shortCode: 'bk' },
        guest_experience:         { try: 99,  eur: 10, displayName: 'Guest Experience', shortCode: 'ge' },
        housekeeping_coordinator: { try: 99,  eur: 10, displayName: 'Housekeeping',  shortCode: 'hk' },
      };

      shortCode values are 2-letter codes for Telegram callback_data (64-byte limit per research).

    - Add helper: export function calculateMonthlyTotal(roles: EmployeeRoleKey[], currency: 'try' | 'eur'): number
      Sums EMPLOYEE_ROLE_PRICES[role][currency] for each role in the array.

    Update src/lib/billing/enforcement.ts:
    - Keep existing enforceAgentLimit function unchanged (it still works for backward compat with tier-based plans)
    - This function will be replaced in a later phase when per-employee enforcement is fully wired; for now the tier system coexists

    Update src/types/database.ts:
    - Add owner_telegram_chat_id: number | null to Hotel interface
    - Add trial_notified_day_7: boolean, trial_notified_day_12: boolean, trial_notified_day_13: boolean, trial_notified_day_14: boolean to Subscription interface
    - Add these new columns to the Database.public.Tables.hotels and Database.public.Tables.subscriptions entries (Insert/Update types)
  </action>
  <verify>
    Run: pnpm exec tsc --noEmit
    Verify zero TypeScript errors.
    Verify 0011_billing_v2.sql exists and contains ALTER TABLE statements for both hotels and subscriptions.
    Verify plans.ts exports EMPLOYEE_ROLE_PRICES and calculateMonthlyTotal.
  </verify>
  <done>
    Migration adds owner_telegram_chat_id to hotels and 4 trial notification tracking booleans to subscriptions. EMPLOYEE_ROLE_PRICES defines per-role pricing with TRY/EUR amounts and 2-letter short codes. TypeScript types updated to match.
  </done>
</task>

<task type="auto">
  <name>Task 2: Persist owner Telegram chat_id in wizard completion</name>
  <files>
    src/lib/telegram/wizard/wizardActions.ts
  </files>
  <action>
    In src/lib/telegram/wizard/wizardActions.ts, modify the completeWizard function:

    The function already receives chatId (number) as first parameter and has access to the supabase service client. After the existing .update({ onboarding_completed_at: ... }) call, add owner_telegram_chat_id to the same update:

    Change the update from:
      .update({ onboarding_completed_at: new Date().toISOString() })
    To:
      .update({ onboarding_completed_at: new Date().toISOString(), owner_telegram_chat_id: chatId })

    This persists the hotel owner's Telegram chat_id to the hotels table, which the trial notification cron (Plan 02) needs to send countdown messages.

    The chatId parameter is already available in completeWizard's signature — no new data flow needed. The column was added in Task 1's migration.

    IMPORTANT: Do NOT change any other part of wizardActions.ts. The only change is adding owner_telegram_chat_id to the existing update call inside completeWizard.
  </action>
  <verify>
    Run: pnpm exec tsc --noEmit
    Verify zero TypeScript errors.
    Grep for "owner_telegram_chat_id" in wizardActions.ts to confirm the field is in the update call.
  </verify>
  <done>
    completeWizard persists the owner's Telegram chat_id alongside onboarding_completed_at. The cron notification system can now look up owner_telegram_chat_id from the hotels table to send trial countdown messages via Telegram.
  </done>
</task>

</tasks>

<verification>
1. `pnpm exec tsc --noEmit` passes with zero errors
2. Migration file 0011_billing_v2.sql contains ALTER TABLE for both hotels (owner_telegram_chat_id) and subscriptions (4 notification booleans)
3. plans.ts exports EMPLOYEE_ROLE_PRICES with 4 roles, each having try/eur/displayName/shortCode
4. plans.ts exports calculateMonthlyTotal helper
5. database.ts Hotel interface includes owner_telegram_chat_id: number | null
6. database.ts Subscription interface includes trial_notified_day_7/12/13/14: boolean
7. wizardActions.ts completeWizard update call includes owner_telegram_chat_id: chatId
</verification>

<success_criteria>
- Per-employee pricing constants are defined and exported
- Database schema supports chat_id persistence and trial notification tracking
- Wizard completion persists the owner's Telegram chat_id
- All existing functionality remains unchanged (backward compatible)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/12-billing-model-migration-and-trial-end-flow/12-01-SUMMARY.md`
</output>
