---
phase: 12-billing-model-migration-and-trial-end-flow
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/lib/telegram/trialCallback.ts
  - src/app/api/telegram/[slug]/route.ts
autonomous: true
requirements: [PRIC-03, PRIC-04, PRIC-05]

must_haves:
  truths:
    - "Hotel owner receives an inline keyboard showing each employee with price after trial ends"
    - "Owner can toggle employees on/off and see running total update"
    - "On confirm, owner receives a payment link for the correct total amount"
    - "Unselected employees' bots are deactivated immediately after selection"
    - "Selected employees continue working after payment completes"
  artifacts:
    - path: "src/lib/telegram/trialCallback.ts"
      provides: "Callback handler for trial_toggle and trial_confirm callbacks"
      exports: ["handleTrialCallback"]
    - path: "src/app/api/telegram/[slug]/route.ts"
      provides: "Extended webhook handler routing callback_query to trial handler"
      contains: "callback_query"
  key_links:
    - from: "src/app/api/telegram/[slug]/route.ts"
      to: "src/lib/telegram/trialCallback.ts"
      via: "import handleTrialCallback, dispatch on callback_query.data prefix"
      pattern: "handleTrialCallback"
    - from: "src/lib/telegram/trialCallback.ts"
      to: "src/lib/billing/trialSelection.ts"
      via: "import get/set/clearTrialSelection"
      pattern: "TrialSelection"
    - from: "src/lib/telegram/trialCallback.ts"
      to: "src/lib/billing/plans.ts"
      via: "import EMPLOYEE_ROLE_PRICES, calculateMonthlyTotal"
      pattern: "EMPLOYEE_ROLE_PRICES"
    - from: "src/lib/telegram/trialCallback.ts"
      to: "hotel_bots.is_active"
      via: "supabase update to deactivate unselected bots"
      pattern: "is_active.*false"
---

<objective>
Build the trial-end employee selection flow: inline keyboard for toggling employees, payment link generation, and bot deactivation for unselected employees.

Purpose: When the trial ends, hotel owners must choose which AI employees to keep. This plan creates the interactive Telegram flow where owners see each employee with its price, toggle selections, confirm, receive a payment link for the correct total, and have unselected bots deactivated immediately.

Output: Trial callback handler, extended employee bot webhook to handle callback_query. (Redis selection state and inline keyboard builder are created in Plan 12-01 as shared foundation.)
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-billing-model-migration-and-trial-end-flow/12-RESEARCH.md
@.planning/phases/12-billing-model-migration-and-trial-end-flow/12-01-SUMMARY.md
@.planning/phases/06-billing/06-02-SUMMARY.md
@.planning/phases/06-billing/06-03-SUMMARY.md
@.planning/phases/09-telegram-infrastructure/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trial callback handler and webhook extension for callback_query</name>
  <files>
    src/lib/telegram/trialCallback.ts
    src/app/api/telegram/[slug]/route.ts
  </files>
  <action>
    Create src/lib/telegram/trialCallback.ts:

    export async function handleTrialCallback(
      callbackQuery: TelegramCallbackQuery,
      botRow: { hotel_id: string; vault_secret_id: string; role: string }
    ): Promise<void>

    Import TelegramCallbackQuery from types.ts. Import get/set/clearTrialSelection from trialSelection.ts. Import buildSelectionKeyboard from trialKeyboard.ts. Import EMPLOYEE_ROLE_PRICES, calculateMonthlyTotal, EmployeeRoleKey from plans.ts. Import getProviderForHotel from plans.ts. Import createServiceClient.

    Always call answerCallbackQuery FIRST (same pattern as wizard callback handler — dismiss loading spinner before async work).

    Handle callback_data patterns:

    1. "trial_toggle:{shortCode}" (e.g. "trial_toggle:fd"):
       - Get TrialSelection from Redis for this chatId
       - If no selection state, return silently (expired or not initiated)
       - Find the role key matching the shortCode via EMPLOYEE_ROLE_PRICES lookup
       - Toggle: if role is in selectedRoles, remove it; if not, add it
       - Must have at least 1 role selected — if removal would make selectedRoles empty, answerCallbackQuery with text "You must keep at least one employee" and return without toggling
       - Update Redis state with new selectedRoles
       - Build new keyboard with buildSelectionKeyboard
       - Build new message text showing updated selections and total
       - Call Telegram editMessageText API to update the existing message (use messageId from TrialSelection state) with new text AND new reply_markup (keyboard)

    2. "trial_all":
       - Get TrialSelection from Redis
       - Set selectedRoles = availableRoles (select all)
       - Update Redis state
       - editMessageText with updated keyboard and total

    3. "trial_confirm":
       - Get TrialSelection from Redis
       - If no state, return silently
       - If selectedRoles is empty, answerCallbackQuery with "Select at least one employee" and return
       - Calculate total using calculateMonthlyTotal(selectedRoles, currency)
       - Deactivate unselected bots: for each role in availableRoles that is NOT in selectedRoles:
         UPDATE hotel_bots SET is_active = false WHERE hotel_id = X AND role = Y
         Use service client with SupabaseClient cast
       - Generate payment link based on provider:
         - Query hotels table for country to determine provider via getProviderForHotel(country)
         - For Mollie (EU): use mollieClient.paymentLinks.create() from @mollie/api-client
           Amount: { currency: 'EUR', value: total.toFixed(2) }
           Description: "OtelAI - {N} AI employees"
           redirectUrl: process.env.NEXT_PUBLIC_APP_URL + '/billing?status=success'
           Set metadata: { hotelId, selectedRoles: selectedRoles.join(',') }
           Extract paymentLink._links.paymentLink.href as the link URL
         - For iyzico (TR): generate a link to the web dashboard billing page instead:
           paymentUrl = process.env.NEXT_PUBLIC_APP_URL + '/billing?action=subscribe&roles=' + selectedRoles.join(',') + '&total=' + total
           (Per research: iyzico Checkout Form requires buyer object with Turkish national ID — cannot generate a direct payment link. Send owner to web dashboard billing page which collects buyer info and calls iyzico checkout API)
       - Send Telegram message to chatId with the payment link:
         "Your selection: {selectedRoles as display names}\nMonthly total: {total} {currency}\n\nComplete payment here: {paymentUrl}\n\nUnselected employees have been deactivated. They will reactivate after payment is confirmed."
       - Clear Redis trial selection state
       - editMessageText on the original selection message to show "Selection confirmed. Proceed to payment." (removes the inline keyboard)

    Error handling: Wrap each handler section in try/catch. Log errors, never throw. answerCallbackQuery should be called even on error paths.

    For Mollie payment link: import { mollieClient } from '@/lib/billing/mollie'. The Mollie Payment Links API creates a reusable payment link. Check if mollieClient.paymentLinks exists; if not, fall back to creating a one-time payment via mollieClient.payments.create with redirectUrl.

    ---

    Modify src/app/api/telegram/[slug]/route.ts to handle callback_query:

    Currently the webhook handler only processes message.text and discards everything else. Extend it to also handle callback_query updates:

    1. After parsing the body as TelegramUpdate, check for body.callback_query in addition to body.message.
    2. If callback_query exists and has .data starting with "trial_" prefix:
       - Inside the existing after() block (or a new after() block), call handleTrialCallback(callbackQuery, botRow)
    3. The early exit for non-text messages should be updated: instead of returning 200 when !message?.text, check for callback_query first.

    Updated flow:
    - Parse body
    - If message?.text → existing agent flow (unchanged)
    - Else if callback_query?.data?.startsWith('trial_') → route to handleTrialCallback in after()
    - Else → return 200 silently (photos, stickers, etc.)

    Import handleTrialCallback from '@/lib/telegram/trialCallback'.
    Import TelegramCallbackQuery — it's already in types.ts from Phase 11.

    IMPORTANT: The existing message handling flow must remain completely unchanged. Only ADD the callback_query branch. The after() pattern must wrap the trial callback handler too (return 200 before processing).
  </action>
  <verify>
    Run: pnpm exec tsc --noEmit
    Verify zero TypeScript errors.
    Verify trialCallback.ts exports handleTrialCallback.
    Grep for "trial_toggle" in trialCallback.ts.
    Grep for "callback_query" in [slug]/route.ts to confirm the webhook extension.
    Grep for "handleTrialCallback" in [slug]/route.ts to confirm the import.
  </verify>
  <done>
    Trial callback handler processes toggle, select-all, and confirm actions. Confirm deactivates unselected bots and generates a payment link (Mollie Payment Links API for EU, web dashboard URL for iyzico TR). Employee bot webhook extended to route callback_query with trial_ prefix to the handler.
  </done>
</task>

</tasks>

<verification>
1. `pnpm exec tsc --noEmit` passes with zero errors
2. trialCallback.ts handles trial_toggle, trial_all, and trial_confirm callback_data
3. trialCallback.ts imports get/set/clearTrialSelection from trialSelection.ts (created in Plan 12-01)
4. trialCallback.ts imports buildSelectionKeyboard from trialKeyboard.ts (created in Plan 12-01)
5. trial_confirm deactivates unselected bots (hotel_bots.is_active = false)
6. trial_confirm generates Mollie payment link (EU) or iyzico web dashboard URL (TR)
7. [slug]/route.ts routes callback_query with trial_ prefix to handleTrialCallback
8. Existing message handling in [slug]/route.ts is unchanged
9. All after() patterns used for async work (200 returned before processing)
</verification>

<success_criteria>
- Hotel owner can toggle employee selections via inline keyboard
- Running price total updates on each toggle
- Confirm deactivates unselected bots immediately
- Confirm generates correct payment link based on provider (Mollie/iyzico)
- Employee bot webhook handles callback_query without breaking existing message flow
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/12-billing-model-migration-and-trial-end-flow/12-03-SUMMARY.md`
</output>
