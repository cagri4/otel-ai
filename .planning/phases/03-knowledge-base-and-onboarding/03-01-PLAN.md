---
phase: 03-knowledge-base-and-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0003_knowledge_base.sql
  - src/types/database.ts
  - src/lib/validations/knowledge.ts
  - src/lib/actions/knowledge.ts
  - src/lib/agents/memory.ts
  - src/lib/agents/assembleContext.ts
autonomous: true
requirements:
  - KNOW-01
  - KNOW-02
  - KNOW-03
  - KNOW-04
  - ONBR-04

must_haves:
  truths:
    - "New hotel signup seeds default boutique hotel facts (check-in 3pm, checkout 11am, standard policies) and a default room via Postgres trigger"
    - "Room data (name, type, bed_type, max_occupancy, description, amenities, price_note) is stored in a dedicated rooms table with RLS"
    - "Server Actions can add, update, and delete hotel_facts and rooms rows (RLS-scoped)"
    - "Agent system prompt includes room information alongside semantic facts in the memory layer"
    - "Knowledge base categories include recommendation for local tips"
  artifacts:
    - path: "supabase/migrations/0003_knowledge_base.sql"
      provides: "rooms table, RLS policies, seed_hotel_defaults trigger, onboarding_completed_at column"
      contains: "CREATE TABLE public.rooms"
    - path: "src/types/database.ts"
      provides: "Room and extended HotelFactCategory types"
      contains: "interface Room"
    - path: "src/lib/validations/knowledge.ts"
      provides: "Zod schemas for fact and room forms"
      contains: "factSchema"
    - path: "src/lib/actions/knowledge.ts"
      provides: "Server Actions for knowledge base CRUD"
      exports: ["addFact", "updateFact", "deleteFact", "addRoom", "updateRoom", "deleteRoom"]
    - path: "src/lib/agents/memory.ts"
      provides: "loadRoomContext function for agent context assembly"
      contains: "loadRoomContext"
    - path: "src/lib/agents/assembleContext.ts"
      provides: "Room context injected into memory layer"
      contains: "loadRoomContext"
  key_links:
    - from: "src/lib/agents/assembleContext.ts"
      to: "src/lib/agents/memory.ts"
      via: "loadRoomContext() call in Promise.all"
      pattern: "loadRoomContext\\(hotelId\\)"
    - from: "src/lib/actions/knowledge.ts"
      to: "supabase/migrations/0003_knowledge_base.sql"
      via: "Server Actions query rooms and hotel_facts tables"
      pattern: "from\\('rooms'\\)"
    - from: "supabase/migrations/0003_knowledge_base.sql"
      to: "supabase/migrations/0002_agent_core.sql"
      via: "seed trigger inserts into hotel_facts (created in 0002)"
      pattern: "INSERT INTO public.hotel_facts"
---

<objective>
Create the knowledge base data layer: rooms table, default data seeding trigger, TypeScript types, Zod validation schemas, CRUD Server Actions, and agent context integration.

Purpose: Establishes the complete backend for the knowledge base — schema, types, validation, mutations, and agent context pipeline — so the UI plan (03-02) and onboarding plan (03-03) have a solid foundation.

Output: Migration SQL, TypeScript types, Zod schemas, Server Actions, and extended agent context assembly.
</objective>

<execution_context>
@/home/cagr/.claude/get-shit-done/workflows/execute-plan.md
@/home/cagr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-knowledge-base-and-onboarding/03-RESEARCH.md

# Prior phase artifacts (direct dependencies)
@supabase/migrations/0001_foundation.sql
@supabase/migrations/0002_agent_core.sql
@src/types/database.ts
@src/lib/agents/memory.ts
@src/lib/agents/assembleContext.ts
@src/app/(dashboard)/settings/actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rooms table migration and default data seed trigger</name>
  <files>supabase/migrations/0003_knowledge_base.sql</files>
  <action>
Create migration `supabase/migrations/0003_knowledge_base.sql` with:

1. **`onboarding_completed_at` column on hotels table** — add `ALTER TABLE public.hotels ADD COLUMN onboarding_completed_at TIMESTAMPTZ DEFAULT NULL;` This is the onboarding gate (research open question 1 recommendation: dedicated column, not city check).

2. **`rooms` table** — per research Pattern 1 schema exactly:
   - id UUID PK DEFAULT gen_random_uuid()
   - hotel_id UUID NOT NULL REFERENCES hotels(id) ON DELETE CASCADE
   - name TEXT NOT NULL (e.g. "Deluxe Ocean View")
   - room_type TEXT NOT NULL (e.g. "standard", "deluxe", "suite")
   - bed_type TEXT (nullable: "king", "twin", "queen")
   - max_occupancy INTEGER (nullable)
   - description TEXT (nullable)
   - amenities TEXT[] (PostgreSQL text array for amenity strings)
   - base_price_note TEXT (nullable, e.g. "from $120/night" — text for agent, not booking engine)
   - sort_order INTEGER NOT NULL DEFAULT 0
   - created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
   - updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()

3. **Index:** `CREATE INDEX idx_rooms_hotel_id ON public.rooms(hotel_id);`

4. **RLS on rooms** — enable RLS, create 4 policies (SELECT/INSERT/UPDATE/DELETE) using the same JWT pattern as hotel_facts:
   - `hotel_id = ((SELECT auth.jwt()) ->> 'hotel_id')::uuid`
   - Name policies: "Hotel staff see own rooms", "Hotel staff insert own rooms", "Hotel staff update own rooms", "Hotel staff delete own rooms"

5. **Updated_at trigger on rooms:** `CREATE TRIGGER set_rooms_updated_at BEFORE UPDATE ON public.rooms FOR EACH ROW EXECUTE PROCEDURE public.set_updated_at();` (reuses function from 0001_foundation.sql)

6. **`seed_hotel_defaults()` function and trigger** — per research Pattern 2 exactly:
   - SECURITY DEFINER SET search_path = '' (same as handle_new_user)
   - Inserts 9 default hotel_facts rows: 5 policies (check-in 3pm, checkout 11am, no-smoking, cancellation 48h, pets), 3 FAQs (WiFi, 24h front desk, breakfast), 1 amenity (concierge)
   - Inserts 1 default room skeleton (Standard Room, room_type='standard', sort_order=1)
   - Trigger: `CREATE TRIGGER on_hotel_created_seed_defaults AFTER INSERT ON public.hotels FOR EACH ROW EXECUTE PROCEDURE public.seed_hotel_defaults();`

Add a comment at the top: `-- Depends on: 0001_foundation.sql (hotels, set_updated_at), 0002_agent_core.sql (hotel_facts)`
  </action>
  <verify>
Read the migration file and verify:
- rooms table has all columns with correct types
- RLS policies match hotel_facts pattern
- seed_hotel_defaults function inserts correct default rows
- onboarding_completed_at column added to hotels
- set_rooms_updated_at trigger defined

Run: `cd /home/cagr/Masaüstü/otel-ai && npx tsc --noEmit` (should still pass — migration is SQL only)
  </verify>
  <done>Migration file exists at supabase/migrations/0003_knowledge_base.sql with rooms table, RLS, seed trigger, and onboarding_completed_at column. TypeScript compilation unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript types, Zod schemas, and CRUD Server Actions</name>
  <files>
src/types/database.ts
src/lib/validations/knowledge.ts
src/lib/actions/knowledge.ts
  </files>
  <action>
**1. Extend `src/types/database.ts`:**

- Add `'recommendation'` to `HotelFactCategory` union type: `export type HotelFactCategory = 'policy' | 'amenity' | 'faq' | 'pricing_note' | 'recommendation';`
- Add `Room` interface:
  ```
  interface Room {
    id: string;
    hotel_id: string;
    name: string;
    room_type: string;
    bed_type: string | null;
    max_occupancy: number | null;
    description: string | null;
    amenities: string[] | null;
    base_price_note: string | null;
    sort_order: number;
    created_at: string;
    updated_at: string;
  }
  ```
- Add `onboarding_completed_at: string | null;` to the `Hotel` interface (after `contact_phone`)
- Add `rooms` to the `Database` type Tables section following the same pattern as hotel_facts (Row/Insert/Update/Relationships)

**2. Create `src/lib/validations/knowledge.ts`:**

- Export `FACT_CATEGORIES = ['policy', 'faq', 'amenity', 'pricing_note', 'recommendation'] as const`
- Export `FactCategory` type from the const
- Export `factSchema = z.object({ category: z.enum(FACT_CATEGORIES), fact: z.string().min(5, 'Fact must be at least 5 characters').max(500, 'Fact must be under 500 characters') })`
- Export `roomSchema = z.object({ name: z.string().min(2, 'Room name required').max(100), room_type: z.string().min(1, 'Room type required'), bed_type: z.string().optional(), max_occupancy: z.coerce.number().int().min(1).max(20).optional(), description: z.string().max(1000).optional(), amenities: z.string().optional() (comma-separated string, split on server), base_price_note: z.string().max(100).optional() })`

**3. Create `src/lib/actions/knowledge.ts`:**

'use server' module with 6 Server Actions. Follow the exact pattern in `src/app/(dashboard)/settings/actions.ts` — auth check via getUser(), get hotel_id via RLS-scoped hotels query, Zod validation, Supabase CRUD, revalidatePath('/knowledge').

Functions:
- `addFact(formData: FormData)` — parse with factSchema, insert into hotel_facts with hotel_id, return `{ success: true }` or `{ error: string }`
- `updateFact(id: string, formData: FormData)` — parse with factSchema, update hotel_facts where id = id, revalidatePath
- `deleteFact(id: string)` — delete from hotel_facts where id = id, revalidatePath
- `addRoom(formData: FormData)` — parse with roomSchema, split amenities string by comma into string[], insert into rooms with hotel_id. Handle the SupabaseClient cast same as memory.ts pattern for INSERT
- `updateRoom(id: string, formData: FormData)` — parse with roomSchema, update rooms where id = id
- `deleteRoom(id: string)` — delete from rooms where id = id

Each function:
1. `const supabase = await createClient()`
2. `const { data: { user } } = await supabase.auth.getUser()` — return error if no user
3. Get hotel_id: `const { data: hotel } = await supabase.from('hotels').select('id').single()` (RLS scopes to user's hotel)
4. Validate with appropriate Zod schema
5. Execute Supabase query (INSERT uses the SupabaseClient unknown cast; UPDATE/DELETE use regular client with .eq('id', id))
6. `revalidatePath('/knowledge')`
7. Return `{ success: true }` or `{ error: string }`

Use the same cast pattern for INSERT as established: `(supabase as unknown as SupabaseClient).from('table').insert(payload as Record<string, unknown>)`

Import SupabaseClient type from '@supabase/supabase-js'.
  </action>
  <verify>
Run: `cd /home/cagr/Masaüstü/otel-ai && npx tsc --noEmit`

Verify:
- No TypeScript errors
- Room type exists in database.ts
- factSchema and roomSchema export from validations/knowledge.ts
- All 6 Server Actions export from actions/knowledge.ts
- HotelFactCategory includes 'recommendation'
- Hotel interface includes onboarding_completed_at
  </verify>
  <done>TypeScript types extended with Room and onboarding_completed_at, Zod schemas created for fact and room validation, 6 CRUD Server Actions created for hotel_facts and rooms. All compile without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Agent context integration — loadRoomContext and assembleContext extension</name>
  <files>
src/lib/agents/memory.ts
src/lib/agents/assembleContext.ts
  </files>
  <action>
**1. Add `loadRoomContext()` to `src/lib/agents/memory.ts`:**

Add a new exported function after the `loadSemanticFacts` section (before Tier 2 — Episodic Memory):

```typescript
export async function loadRoomContext(hotelId: string): Promise<string> {
  const supabase = await createClient();

  const { data, error } = await supabase
    .from('rooms')
    .select('*')
    .eq('hotel_id', hotelId)
    .order('sort_order')
    .returns<Room[]>();

  if (error || !data || data.length === 0) return '';

  const lines = data.map((room) => {
    const parts = [`ROOM: ${room.name} (${room.room_type})`];
    if (room.bed_type) parts.push(`${room.bed_type} bed`);
    if (room.max_occupancy) parts.push(`max ${room.max_occupancy} guests`);
    if (room.base_price_note) parts.push(room.base_price_note);
    const header = parts.join(' — ');
    const details = [room.description, room.amenities?.join(', ')].filter(Boolean).join('. ');
    return details ? `${header}. ${details}` : header;
  });

  return lines.join('\n');
}
```

Import `Room` from `@/types/database` at the top of the file.

**2. Extend `src/lib/agents/assembleContext.ts`:**

- Import `loadRoomContext` from `./memory` alongside the existing imports
- In `assembleSystemPrompt()`, extend the Promise.all to include `loadRoomContext(hotelId)`:
  ```typescript
  const [hotel, semanticFacts, roomContext, episodicHistory] = await Promise.all([
    fetchHotel(hotelId),
    loadSemanticFacts(hotelId),
    loadRoomContext(hotelId),
    loadEpisodicHistory(hotelId, config.memoryScope),
  ]);
  ```
- After the `semanticFacts` check in the memory layer assembly, add:
  ```typescript
  if (roomContext.trim()) {
    memoryParts.push(`Room Information:\n${roomContext}`);
  }
  ```

This ensures KNOW-04 is satisfied: room data is injected into the agent's system prompt via the `<memory>` layer. No agent code changes needed — the existing pipeline handles it.
  </action>
  <verify>
Run: `cd /home/cagr/Masaüstü/otel-ai && npx tsc --noEmit`

Verify:
- loadRoomContext is exported from memory.ts
- assembleContext.ts fetches room context in parallel
- Room data appears in the memory layer string
  </verify>
  <done>Agent context pipeline extended: loadRoomContext fetches rooms and formats as readable text; assembleContext.ts injects room info into the memory layer alongside semantic facts. All AI employees now draw from room data automatically (KNOW-04).</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Migration file at `supabase/migrations/0003_knowledge_base.sql` contains rooms table, 4 RLS policies, seed trigger, updated_at trigger, and onboarding_completed_at column
3. `src/types/database.ts` contains Room interface, extended HotelFactCategory, onboarding_completed_at on Hotel, and rooms in Database type
4. `src/lib/validations/knowledge.ts` exports factSchema and roomSchema
5. `src/lib/actions/knowledge.ts` exports addFact, updateFact, deleteFact, addRoom, updateRoom, deleteRoom
6. `src/lib/agents/memory.ts` exports loadRoomContext
7. `src/lib/agents/assembleContext.ts` includes loadRoomContext in Promise.all and injects into memory layer
</verification>

<success_criteria>
- New hotel signup triggers seed_hotel_defaults, creating 9 default facts + 1 default room
- Room data is stored in a dedicated table with typed columns (not flat text)
- All CRUD operations are type-safe with Zod validation
- Agent system prompt automatically includes room information for guest inquiries
- HotelFactCategory includes 'recommendation' for local tips
</success_criteria>

<output>
After completion, create `.planning/phases/03-knowledge-base-and-onboarding/03-01-SUMMARY.md`
</output>
